feign:
  hystrix:
    enabled: true
mybatis:
  type-aliases-package: com.tfs.demo.model
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: password
    url: jdbc:mysql://localhost:3306/demo?characterEncoding=utf8&useSSL=false
    username: developer
  main:
    allow-bean-definition-overriding: true
  messages:
    basename: messages_jp
    cache-duration: -1
